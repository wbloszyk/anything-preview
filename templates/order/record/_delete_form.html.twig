<form method="post" action="{{ path('app_order_record_delete', {'id': order_record.addedTo.id, 'recordId': order_record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_record.id) }}">
    <button class="btn">Usuń</button>
</form>
